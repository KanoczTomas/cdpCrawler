'use strict';

describe('Graph:', function(){
    it('should have prototype WorkQueue');
    it('should return a promise when this.addNode called');
    it('should call #addToQueue when this.addNode(host) is called');
});